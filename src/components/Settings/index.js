import React from 'react';
import {NavLink} from "react-router-dom";
const Settings = (props) => (
    <div>
        <NavLink to={"/setting/first"} > /setting/first
        </NavLink>
    </div>
);

export default Settings;
